<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight"> <!-- Ripple color, can be a light pastel gray or from theme -->
    <item android:id="@android:id/mask">
        <shape android:shape="oval">
            <solid android:color="@android:color/white" /> <!-- Mask color -->
        </shape>
    </item>
    <item>
        <shape android:shape="oval">
            <!-- Bubbly pastel background color. Using colorSecondaryVariant for differentiation.
                 Or define a new color like @color/pastel_accent_light -->
            <solid android:color="?attr/colorSecondaryVariant" />
            <size android:width="56dp" android:height="56dp"/> <!-- Standard FAB size, adjust as needed -->
        </shape>
    </item>
</ripple>
